## Token Efficiency Guidelines

- Use `rtk ls` to explore content structure.
- Use `rtk read <path>` for markdown files to strip metadata.
- Avoid reading files in `public/` or `node_modules/`.
- Refer to `content/index.md` first before searching all posts.
