4. 반복자 트레이트 (Iterators)
컬렉션(배열, 벡터 등)을 순회할 때 사용합니다. 러스트의 강력한 함수형 프로그래밍 도구입니다.

Iterator: 다음 요소(next())를 가져오는 기능. map, filter, fold 등을 제공.

IntoIterator: 타입을 반복자로 변환 (예: for x in vector).

Extend: 컬렉션에 반복자의 요소를 추가.
